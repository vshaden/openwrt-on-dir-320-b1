Index: xl2tpd-1.3.1/control.c
===================================================================
--- xl2tpd-1.3.1.orig/control.c	2013-08-17 12:15:36.000000000 +0400
+++ xl2tpd-1.3.1/control.c	2013-08-17 12:18:57.000000000 +0400
@@ -799,10 +799,10 @@
             return -EINVAL;
         }
         c->state = ICCN;
-        if (t->fc & SYNC_FRAMING)
-            c->frame = SYNC_FRAMING;
-        else
+        if (t->fc & ASYNC_FRAMING)
             c->frame = ASYNC_FRAMING;
+        else
+            c->frame = SYNC_FRAMING;
 
         buf = new_outgoing (t);
         add_message_type_avp (buf, ICCN);
